# legacy

This folder contains legacy scratch material that should not be part of the active execution path.

Current item:

- `expandable_segments_true/`: empty legacy directory preserved instead of deleted.

