# blocked_routes

Archived routes:

- `dualmap_smoke_docker/`: `DualMap` Docker smoke route. The route executed but did not produce usable object `*.pkl` outputs, so it is not active.
- `openmask3d_smoke_docker/`: `OpenMask3D` Docker/model smoke route blocked by environment/runtime issues.
- `openmask3d_external/`: local `OpenMask3D` source snapshot used for feasibility inspection. This third-party clone is ignored by Git and should be reacquired from the upstream source if needed.

These files are preserved for audit and possible future reactivation. They are not part of the current core reproduction path.
